services:
  spring-server:
    image: zzang6535/gamyeon-spring:latest
    container_name: SPRING-SERVER
    env_file:
      - .env
    ports:
      - "8080:8080"
    networks:
      - gamyeon-net

networks:
  gamyeon-net:
    external: true
