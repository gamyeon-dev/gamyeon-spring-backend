services:
  spring-server:
    image: zzang6535/gamyeon-spring:latest
    container_name: SPRING-SERVER
    environment:
      - CONSUL_HOST=consul
    env_file:
      - .env
    ports:
      - "8080:8080"
