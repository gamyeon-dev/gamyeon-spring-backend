services:
  spring-server:
    image: zzang6535/gamyeon-spring:latest
    container_name: SPRING-SERVER
    ports:
      - "8080:8080"
    environment:
      - CONSUL_HOST=consul
    networks:
      - gamyeon-net

networks:
  gamyeon-net:
    external: true
